# Compiled class files
*.class

# Jar, War, Ear, and other archive files
*.jar
*.war
*.ear
*.zip
*.tar
*.gz
*.rar

# Maven build directory
/target/
# Gradle build directories
/build/
.gradle/

# Maven/Gradle Wrapper files (usually included in projects, but sometimes generated/cached)
# You might want to include these if your team needs the wrapper to build without installing Maven/Gradle globally.
# If you decide to ignore them:
# !/mvnw
# !/mvnw.cmd
# !/.mvn/wrapper/maven-wrapper.jar
# !/gradlew
# !/gradlew.bat
# !/.gradle/wrapper/gradle-wrapper.jar

# Log files
*.log
logs/

# Temporary files and backup files
*.tmp
*.bak
*~
*.swp
*.swo

# IDE-specific files and directories
# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
/out/ # IntelliJ output directory for compiled classes

# Eclipse
.project
.classpath
.settings/
/bin/ # Eclipse output directory for compiled classes
.metadata/
tmp/
*.tmp
*.bak
.loadpath
.recommenders/

# VS Code
.vscode/ # You might want to include some files here, like launch.json or settings.json if they are shared team settings.
         # If you want to ignore specific sub-files within .vscode/:
         # .vscode/*.log
         # .vscode/*.bak

# OS-specific files
.DS_Store # macOS
Thumbs.db # Windows
desktop.ini # Windows

# Environment variable files (NEVER commit these if they contain sensitive info!)
.env
.env.local
.env.*.local # For different environments (e.g., .env.development.local)

# Test/Report output directories
/target/surefire-reports/ # Maven Surefire test reports
/target/failsafe-reports/ # Maven Failsafe test reports
/build/reports/ # Gradle test reports
/build/test-results/ # Gradle test results

# Code coverage reports
/target/site/jacoco/ # JaCoCo reports (Maven)
/build/jacoco/ # JaCoCo reports (Gradle)
/coverage/

# Documentation generation (e.g., Javadoc)
/target/apidocs/
/build/docs/

# Specific files generated by some tools (e.g., Lombok)
# If you are using Lombok and encounter issues with generated files being tracked
# You might see files ending with *.class after compilation that are not in target/ or build/
# This is usually not needed as they should be compiled into /target/ or /build/

# Miscellaneous
# Any other directories created by specific tools or frameworks that should not be versioned